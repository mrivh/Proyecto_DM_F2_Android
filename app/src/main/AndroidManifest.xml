<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lifexperience">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".AllUsers"
        android:theme="@style/Theme.LifeXperience">
        <activity android:name=".Descriptions" />
        <activity android:name=".Experiences" />
        <activity android:name=".Activities" />
        <activity android:name=".Register" />
        <activity android:name=".PlaceActivity" />
        <activity android:name=".MainMenu" />
        <activity android:name=".GuideProfile" />
        <activity android:name=".TravelerProfile" />
        <activity android:name=".TravelerProfile" />
        <activity android:name=".DetailActivityPlaces" />
        <activity android:name=".DetailActivityExperience"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>