<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lifexperience">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeXperience">
        <activity android:name=".Descripciones" />
        <activity android:name=".Experiencias" />
        <activity android:name=".Actividades" />
        <activity android:name=".Registro" />
        <activity android:name=".Lugar" />
        <activity android:name=".Inicio" />
        <activity android:name=".Perfil_guia" />
        <activity android:name=".Perfil_Usuario" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>